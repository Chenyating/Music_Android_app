<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jimihua.work">

    <!--  -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--  -->


    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_photo1"
        android:label="鸡米花"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomePage" />
        <activity android:name=".PlayPage" />
        <activity
            android:name=".music_list"
            android:label="@string/title_activity_music_list" />
        <activity android:name=".login"></activity>
        <activity android:name=".register" />
        <activity
            android:name=".changepassword"
            android:label="@string/title_activity_changepassword"></activity>
        <activity
            android:name=".searchpassword"
            android:label="@string/title_activity_searchpassword"></activity>
        <activity android:name=".tiaozhuan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".like_musiclist"
            android:label="@string/title_activity_like_musiclist"></activity>
        <activity android:name=".Record_Page"></activity>
    </application>

</manifest>